theme: jekyll-theme-cayman

defaults:
  -
    scope:
      path: "_docs"
      type: "docs"
    values:
      layout: "docs"

collections:
  docs:
    permalink: /:collection/:path/
    output: true

name: autospec name heeere

plugins:
  - jekyll-sitemap